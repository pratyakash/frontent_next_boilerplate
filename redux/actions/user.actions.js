import { simpleDispatch_get } from "./helper.actions";
import { apiConstants } from "../../utils/api.paths";

export const getAllUsers = (user) => (dispatch) => {
  //   let URL = apiConstants.END_POINTS.API + apiConstants.END_POINTS.GET_USERS.POINT;
  //   simpleDispatch_get(dispatch, URL, actionTypes.USER_LIST_GET_SUCCESS);
};
